To run the app, install and make sure activemq is running (find the details here: https://activemq.apache.org/version-5-installation) and
run the test file inside the test package from the app.

*The tests currently fail
